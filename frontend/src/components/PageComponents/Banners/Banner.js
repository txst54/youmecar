import React from "react";

export default function EventPage(props) {
    return (
        <section>
            <div>{props.title}</div>
            <div>{propsd.subtitle}</div>
        </section>
    );
}