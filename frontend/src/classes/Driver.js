class Driver {

    constructor(name, carDescription, licensePlate, seatCapacity) {
        this.name = name;
        this.carDescription = carDescription;
        this.licensePlate = licensePlate;
        this.seatCapacity = seatCapacity;
    }
    
}